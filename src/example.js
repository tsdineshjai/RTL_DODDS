const username = 'freddy'
typeof username === 'string'

if (!('servicework' in navigator)) {
  //you have an old broswe :-(
}

const greeting = 'hello'

console.log(`${greeting} world!`)
;[1, 2, 3].forEach((eachItem) => console.log(eachItem))
